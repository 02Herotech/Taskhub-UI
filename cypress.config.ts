export default {
  e2e: {
  setupNodeEvents(on: functionType, config: ConfigType) {
      // implement node event listeners here
    },
  },
};
type functionType = (/* Specify the parameter and return types for 'on' */ any) => any;
type ConfigType = {
  // Define the type for 'config' here
};
